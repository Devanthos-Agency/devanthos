---
import Layout from '@/layouts/Layout.astro';
import Button from '@/components/ui/Button.astro';
import { DevanthosIcon } from '@/components/icons';
import { Home, ArrowLeft, Search } from 'lucide-react';
---

<Layout
    title="404 - Página no encontrada | Devanthos"
    description="La página que buscas no existe. Regresa al inicio o explora nuestros servicios de desarrollo web y marketing digital.">
    <section class="relative flex min-h-screen items-center justify-center px-4 pt-20 pb-12">
        <!-- Decorative background elements -->
        <div
            class="absolute top-0 right-0 bottom-auto left-auto h-[500px] w-[500px] -translate-x-[30%] -translate-y-[20%] rounded-full bg-[rgba(173,109,244,0.3)] opacity-30 blur-[100px]">
        </div>
        <div
            class="absolute bottom-0 left-0 h-[300px] w-[300px] translate-x-[10%] translate-y-[20%] rounded-full bg-[rgba(109,173,244,0.3)] opacity-30 blur-[80px]">
        </div>

        <div class="relative z-10 mx-auto max-w-4xl text-center">
            <!-- 404 Number with gradient -->
            <div class="mb-8">
                <h1
                    class="from-primary via-primary/80 to-primary/60 bg-gradient-to-br bg-clip-text text-[12rem] leading-none font-bold text-transparent opacity-20 select-none md:text-[16rem] lg:text-[20rem]">
                    404
                </h1>
            </div>

            <!-- Error content -->
            <div class="relative -mt-32 space-y-6 md:-mt-40 lg:-mt-48">
                <!-- Logo and brand -->
                <div class="mb-6 flex items-center justify-center gap-2">
                    <DevanthosIcon className="fill-primary size-8" />
                    <span class="font-cocogoose text-foreground text-2xl font-normal">
                        Devanthos
                    </span>
                </div>

                <!-- Main message -->
                <div class="space-y-4">
                    <h2
                        class="font-cocogoose text-foreground text-3xl font-normal md:text-4xl lg:text-5xl">
                        ¡Oops! Página no encontrada
                    </h2>
                    <p class="text-muted-foreground mx-auto max-w-2xl text-lg md:text-xl">
                        Parece que la página que buscas no existe o ha sido movida. No te preocupes,
                        puedes regresar al inicio o explorar nuestros servicios.
                    </p>
                </div>

                <!-- Suggestions -->
                <div class="bg-card/50 mt-12 rounded-xl border p-6 backdrop-blur-sm md:p-8">
                    <h3 class="text-foreground mb-4 text-xl font-semibold">¿Qué puedes hacer?</h3>
                    <div class="grid gap-4 text-left md:grid-cols-3">
                        <div class="space-y-2">
                            <div class="text-primary flex items-center gap-2">
                                <Home className="size-5" />
                                <span class="font-medium">Ir al inicio</span>
                            </div>
                            <p class="text-muted-foreground text-sm">
                                Regresa a nuestra página principal para explorar todos nuestros
                                servicios.
                            </p>
                        </div>
                        <div class="space-y-2">
                            <div class="text-primary flex items-center gap-2">
                                <Search className="size-5" />
                                <span class="font-medium">Buscar servicios</span>
                            </div>
                            <p class="text-muted-foreground text-sm">
                                Explora nuestros servicios de desarrollo web y marketing digital.
                            </p>
                        </div>
                        <div class="space-y-2">
                            <div class="text-primary flex items-center gap-2">
                                <ArrowLeft className="size-5" />
                                <span class="font-medium">Volver atrás</span>
                            </div>
                            <p class="text-muted-foreground text-sm">
                                Regresa a la página anterior usando el botón de tu navegador.
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Action buttons -->
                <div class="mt-8 flex flex-col items-center justify-center gap-4 sm:flex-row">
                    <Button href="/" class="group w-full sm:w-auto">
                        <Home className="mr-2 size-4" />
                        Volver al inicio
                    </Button>

                    <Button href="/servicios" variant="outline" class="w-full sm:w-auto">
                        <Search className="mr-2 size-4" />
                        Ver servicios
                    </Button>

                    <Button href="/contacto" variant="ghost" class="w-full sm:w-auto">
                        Contactar soporte
                    </Button>
                </div>

                <!-- Contact info -->
                <div class="border-border/50 mt-12 border-t pt-8">
                    <p class="text-muted-foreground mb-4">
                        ¿Necesitas ayuda? Nuestro equipo está aquí para ayudarte
                    </p>
                    <div
                        class="flex flex-col items-center justify-center gap-6 text-sm sm:flex-row">
                        <a
                            href="mailto:contacto@devanthos.com"
                            class="text-primary hover:text-primary/80 transition-colors">
                            contacto@devanthos.com
                        </a>
                        <a
                            href="https://wa.me/5492646629632"
                            target="_blank"
                            rel="noopener noreferrer"
                            class="text-primary hover:text-primary/80 transition-colors">
                            WhatsApp: +54 9 2646 62-9632
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Floating elements for visual interest -->
        <div
            class="bg-primary/30 absolute top-1/4 left-10 hidden h-3 w-3 animate-pulse rounded-full lg:block">
        </div>
        <div
            class="absolute top-1/3 right-20 hidden h-2 w-2 animate-pulse rounded-full bg-blue-500/30 lg:block"
            style="animation-delay: 1s;">
        </div>
        <div
            class="absolute bottom-1/4 left-1/4 hidden h-4 w-4 animate-pulse rounded-full bg-purple-500/30 lg:block"
            style="animation-delay: 2s;">
        </div>
        <div
            class="bg-primary/30 absolute right-1/3 bottom-1/3 hidden h-2 w-2 animate-pulse rounded-full lg:block"
            style="animation-delay: 0.5s;">
        </div>
    </section>
</Layout>

<style>
    @keyframes float {
        0%,
        100% {
            transform: translateY(0px);
        }
        50% {
            transform: translateY(-10px);
        }
    }

    .animate-float {
        animation: float 3s ease-in-out infinite;
    }
</style>
